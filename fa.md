# 🚀 ذخیره‌سازی و استریم فایل (Stream Storage)

🌐[English](https://github.com/ussefT/file_stream/blob/main/README.md)

- 📥 دانلود، 📤 آپلود و 🎬 استریم ویدئو یا 🖼️ تصویر بدون نیاز به نصب برنامه اضافی روی موبایل یا کامپیوتر
- 💻 قابل اجرا روی تمامی سیستم‌عامل‌ها (Windows، Linux، macOS)

---

## 🛠️ نصب

### دانلود پروژه
از پروژه یک کلون بگیرید:

```bash
git clone https://github.com/ussefT/file_stream.git
```

### 🪟 ویندوز

ابتدا Python را نصب کنید و از نصب صحیح آن مطمئن شوید:

```cmd
python --help
```

در پوشه پروژه، محیط مجازی ایجاد کنید:

```cmd
python -m venv venv
```

سپس آن را فعال کنید:

```cmd
cd venv\Scripts\
activate
```


---

### 🐧 لینوکس

#### Debian / Ubuntu

```bash
sudo apt install python3-venv -y
```

#### Red Hat / Fedora

```bash
sudo dnf install python3-venv
```

ایجاد محیط مجازی:

```bash
python3 -m venv venv
```

فعال‌سازی محیط:

```bash
source venv/bin/activate
```

---

### 📦 نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

---

## ⚡ استفاده آسان (Easy Use)

اجرای خودکار روی رابط شبکه فعلی (Wi-Fi)

📱 تلفن همراه و کامپیوتر باید به یک شبکه Wi-Fi مشترک متصل باشند.

```bash
python main.py
```

خروجی نمونه:

```text
==================================================
📡 رابط‌های شبکه موجود:
==================================================
  WIFI         : 192.168.43.119
  ETHERNET     : 192.168.56.1, 172.22.224.1
  ALL          : 192.168.56.1, 192.168.43.119, 172.22.224.1
==================================================

✓ نوع رابط شبکه : WiFi
✓ IP سرور       : 192.168.43.119
✓ پورت سرور     : 8001

🚀 سرور در آدرس زیر راه‌اندازی شد:
http://192.168.43.119:8001
```

---

### 🎯 تعیین IP و پورت به‌صورت دستی

```bash
python main.py -l 192.168.56.1 -p 8000
```

---

## 🔧 استفاده پیشرفته (Advanced Usage)

ابتدا آدرس IP سیستم را پیدا کنید:

```bash
ifconfig
```

سپس سرور را روی IP و پورت دلخواه اجرا کنید:

```bash
fastapi dev main.py --localhost 192.168.56.113 --port 8000
```

دسترسی به سرور:

```text
192.168.56.113:8000
```

---

## ✨ نکات

- 📱 روی موبایل از طریق مرورگر به آدرس سرور متصل شوید.
- 🎥 امکان پخش مستقیم فایل‌های ویدئویی وجود دارد.
- 📂 فایل‌ها را می‌توانید مستقیماً آپلود یا دانلود کنید.
- 🌐 تمام دستگاه‌های موجود در شبکه محلی می‌توانند به سرور دسترسی داشته باشند (در صورت مجاز بودن فایروال).
